# Web Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# File Upload
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0          # PostgreSQL 비동기 드라이버
greenlet>=3.0.0          # SQLAlchemy async 필수 의존성 (Python 3.12+ 환경)
alembic>=1.13.0          # DB 마이그레이션

# Image Processing
pillow>=10.3.0

# Settings & Config
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# AI / Vision (YOLOS + CLIP)
transformers>=4.40.0        # Hugging Face 모델 로드 (YOLOS, CLIP)
torch>=2.2.0                # PyTorch 추론 엔진 (CPU)
torchvision>=0.17.0         # 이미지 전처리 유틸
timm>=0.9.0                 # YOLOS 모델 백본 의존성

# Testing
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
